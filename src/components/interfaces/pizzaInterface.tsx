export interface Pizza {
  pizzaId: string;
  name: string;
  description: string;
  type: string;
  imageUrl: string;
  priceRegularSize: number;
  priceMediumSize: number;
  priceLargeSize: number;
}
