export interface Pizza {
  pizzaId: string;
  name: string;
  description: string;
  type: string;
  imageUrl: string;
  priceSmallSize: number;
  priceMediumSize: number;
  priceLargeSize: number;
}
